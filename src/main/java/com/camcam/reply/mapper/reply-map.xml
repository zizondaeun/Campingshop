<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.camcam.reply.mapper.ReplyMapper">

	<select id="replyList" resultType="com.camcam.reply.vo.ReplyVO">
		select * 
		from reply
		where board_no = #{boardNo}
	</select>
	
	<select id="replyListPaging" resultType="com.camcam.reply.vo.ReplyVO" parameterType="com.camcam.reply.vo.ReplyVO">
		select r.*
		from (select rownum rn, a.*
				from (select *
						from reply
						where board_no = #{boardNo}
						order by reply_no desc) a
						<![CDATA[
						where rownum <= (#{rpage} * 5)) r				
						]]>
		where r.rn > (#{rpage} - 1) * 5
	</select>

	<insert id="insertReply"  parameterType="com.camcam.reply.vo.ReplyVO">
		<selectKey resultType="int" keyProperty="replyNo" order="BEFORE">
			select reply_no_seq.nextval from dual
		</selectKey>
		insert into reply(reply_no, board_no, reply)
		values (reply_no_seq.nextval, #{boardNo }, #{reply })	
	</insert>
	
	<delete id="deleteReply" parameterType="int">
		delete from reply where reply_no = #{replyNo}
	</delete>
	
	<select id="selectCount" resultType="int">
		select count(1) from reply
		where board_no = #{boardNo}
	</select>
	
	<update id="updateReply" parameterType="com.camcam.reply.vo.ReplyVO">
		update reply
		set reply = #{reply}
		where reply_no = #{replyNo}
	</update>
	
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.camcam.user.mapper.UserMapper">
	<insert id="insertUser" parameterType="com.camcam.user.vo.UserVO">
		insert into USERS(user_id, password, user_name, email, user_tel, user_resp, address)
		values(#{userId},#{passWord},#{userName},#{email},#{userTel},#{userResp},#{address})
	</insert>

</mapper>